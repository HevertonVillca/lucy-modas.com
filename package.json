{
  "name": "lucy-modas-2",
  "version": "2.0.0",
  "private": true,
  "description": "Lucy Modas 2.0 — loja online",
  "scripts": {
    "deploy": "firebase deploy --only hosting",
    "deploy:all": "firebase deploy",
    "serve": "firebase serve --only hosting",
    "login": "firebase login"
  },
  "devDependencies": {
    "firebase-tools": "^13.35.1"
  }
}
